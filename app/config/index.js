/**
 * Import all configuraton files.
 */

const STRINGS = require('./strings');
const PROMPTS = require('./prompts');
const PACKAGE = require('./package');
const VERSIONS = require('./versions');

module.exports = {
  STRINGS,
  PROMPTS,
  PACKAGE,
  VERSIONS,
};
